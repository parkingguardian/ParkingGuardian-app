// This file exists only to satisfy Expo Router's fallback requirement.
// It should never be used directly — platform-specific versions will override it.

export default function CarParkMap() {
  return null;
}